./gradlew cleanBuildCache
rm -rf ~/.gradle/caches/

./gradlew --stop

rm -rf ~/.gradle/daemon/

./gradlew wrapper --gradle-version=8.10

rm -rf ~/.gradle/caches/
rm -rf .gradle/

./gradlew clean

./gradlew build --refresh-dependencies

java -version

./gradlew --version


